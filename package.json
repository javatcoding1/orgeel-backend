{
  "name": "dentist-appointment-backend",
  "version": "1.0.0",
  "description": "Backend for Dentist Appointment Booking Platform",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "esbuild server.js --bundle --platform=node --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.1.1",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
